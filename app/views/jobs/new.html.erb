
<div class="container">
  <h1 class="">Submit a new job</h1>
  <%= simple_form_for (@job) do |f| %>
    <%= f.input :category %>
    <%= f.input :description %>

      <%= f.submit "Submit Job", class: "btn btn-primary w-100" %>
  <% end %>
</div>
